---
name: reversedd-skill
description: |
  求职者反向背调 / 避坑核查助手。用户输入「公司名 + 岗位 + 城市 + 求职阶段」，
  自动跨平台聚合公开评价（看准 / 职友集 / 牛客 / 黑猫 / **知乎（口碑主力）** / 贴吧 / 微博 / 新闻）并核查硬风险源
  （裁判文书网劳动争议、失信被执行、行政处罚、年报参保人数），同时鼓励用户粘贴在
  小红书看到的避雷帖做交叉验证，最终输出红绿灯风险报告 + 面试反问清单 + 决策建议。
  定位是「求职者避坑核查助手」，不是「避雷帖聚合器」（那是小红书自己的活）。
  触发词：反向背调 / 查公司 / 公司避坑 / 这家公司靠谱吗 / 接 offer 前 / 面经避雷 /
  公司背调 / 求职避坑 / 查一下 XX 公司 / 这家公司能不能去 / 去不去这家。
  即使用户只说「查一下 XX 公司」也应触发。
agent_created: true
---

# 求职者反向背调 · 避坑核查助手

> 平时都是公司背调候选人，这个 skill 帮候选人反向背调公司——
> 把「公司对外话术」翻译成「真实风险」，把「一条避雷帖」验证成「软硬交叉结论」。

## 这个 Skill 能做什么 / 不能做什么

**能做**
- 用 WebSearch 跨公开平台聚合这家公司的口碑与风险信号（看准 / 职友集 / 牛客 / 黑猫 / **知乎（口碑主力）** / 贴吧 / 微博 / 新闻）；小红书无法主动检索，其真实员工口碑由知乎 + 贴吧 + 微博补强
- 核查**硬风险源**（这是杀手锏，普通求职者绝不会自己查）：裁判文书网劳动争议、失信被执行、行政处罚、年报参保人数趋势
- 把用户从**小红书**看到的避雷帖，拿去其他硬源做交叉验证（辨真伪、找硬证据）
- 输出结构化「红绿灯风险报告」+ 按阶段生成的「面试反问清单」+ 「决策建议」

**不能做**
- 不替代小红书/脉脉本身——它拿不到的小红书深度内容，本 skill 也拿不到（纯 WebSearch 对小红书索引稀薄）
- 不预测公司未来、不保证结论 100% 准确（网络信息有噪声、有滞后）
- 不替用户做最终决定，只给「证据 + 风险评级 + 该问什么」

---

## 适用场景与输入

**触发后先澄清输入**（若用户未给全）：
- **必填**：公司名（尽量给工商全称，避免搜到同名不同主体）、岗位、城市、**岗位层级**（基层 / 中层 / 高管）
- **选填**：求职阶段（投前 / 面试中 / 已拿 offer）、用户自己看到的信息（见下）
- ⚠️ **岗位层级是权重开关**：同一家公司，基层岗看重「我会不会被坑」，高管岗看重「这摊子我接不接得动」。层级不同，报告加重的维度不同（见 `references/decision_framework.md` §一·权重规则）。未明确时按「基层」默认，但在报告中注明。

**关于「用户粘贴内容」**：
- 主路径**零粘**——只给公司名也能跑（自动 WebSearch + 硬风险源）。
- 但**强烈鼓励用户粘贴在小红书看到的避雷帖**（截图 / 原文 / 一句「听说加班狠年终奖赖账」都行）。
- 原因：小红书是本 skill 的**天然盲区**（无官方 API、反爬强、WebSearch 索引稀薄），用户刷到的那条它自己大概率搜不到。粘进来后，skill 的价值不是「再读一遍帖」，而是**拿这个信号去硬源交叉验证它真不真、有没有硬证据撑**。

---

## 五步工作流

**① 接收与澄清**
确认公司全称 + 岗位 + 城市 + 阶段 + **岗位层级**（基层/中层/高管）；若用户带小红书避雷帖/朋友爆料，记入「用户情报」栏。
- 层级决定报告重心：基层→「会不会被坑（欠薪/强度/边界）」；中层→「成长空间/管理环境」；高管→「治理/决策权/合规欠账/接盘风险」。

**② 公开多源检索（WebSearch 主干）**
按 `references/search_playbook.md` 的模板与高级语法，跨看准 / 职友集 / 牛客 / 黑猫 / **知乎（口碑主力）** / 贴吧 / 微博 / 新闻 检索（小红书无法主动检索，见 search_playbook 第二节「口碑替代源」）。
- ⚠️ **实体对齐**：搜到的「XX 科技有限公司」参保人数可能只有 30 人，但用户实际雇主是集团子公司。务必区分「法律主体」与「发 offer 的实际主体」，结论里标注清楚。

**③ 硬风险源核查（杀手锏，必做）**
按 `references/risk_sources.md` 核查：裁判文书网劳动争议、失信被执行、行政处罚、年报参保人数。
- **强制先做关系穿透（§0.5）**：先用 WebSearch 系统化搜索词挖出关联壳/子公司/分公司，再逐个查诉讼——绝不止步于母品牌主体。
- 这是最硬的红灯来源，优先级高于任何口碑帖。

**④ 交叉验证与解码**
- 把「口碑信号」与「硬风险信号」对照：小红书说「加班狠」→ 裁判文书网是否有对应加班费官司？看准低分是否一致？黑猫有无欠薪投诉？
- 区分：孤例 vs 普遍、真实吐槽 vs 水军/竞品抹黑。
- 解码公司对外话术（见 `references/interview_questions.md` 附带的黑话提示）。

**⑤ 报告输出**
按 `references/decision_framework.md` 的模板，输出一页红绿灯报告 + 面试反问清单 + 决策建议。

---

## 检索分层策略（三层，连接器皆可选）

```
Tier 0  WebSearch 多源聚合（看准/职友集/牛客/黑猫/**知乎(口碑主力)**/贴吧/微博/新闻 + 硬风险源）  ← 主干，人人可用，零设置；小红书盲区由知乎+贴吧+微博+用户贴链接补强
硬风险   裁判文书网/执行信息网/公示系统（WebSearch 也能触达）
Tier +  用户粘贴小红书避雷帖 / 朋友爆料（可选增强，做交叉验证）
```

**硬数据自动增强层（用户任选其一连接，零成本为主，皆可选）：**
- 关系穿透**默认零门槛 = WebSearch 系统化搜索**（见 `risk_sources.md` §0.5，无需任何连接，人人可用）
- 企百科 MCP / 水滴 MCP（`cisp-mcp`）→ 免费 + 程序化 + 支持股权穿透，作为 WebSearch 挖不全时的增强
- 蚂蚁 MCP → 免费基础工商 + 风险
- 88查 → 网页免费查（手动粘报告）+ 可选付费 API（用户自备 appcode）
- 企查查 / 天眼查连接器 → 最深但付费

- **不内置任何 API key、不要求登录、不依赖 browser-skill**——普通用户零门槛即可跑。
- 任一自动层连接后，硬风险核查（尤其关系穿透）自动拉更全数据；**都不连也完全能跑**（走 WebSearch + 政府官网）。

---

## 预期管理与诚实边界（必须向用户讲清）

在首次输出时，用一两句话做预期管理：
- 本 skill 在**小红书数据上天生弱**（无 API、反爬强）。如果你只想知道「小红书上大家怎么吐槽」，直接刷小红书就好；本 skill 的增量是「帮你验证那些吐槽真假 + 补上你绝不会去查的硬证据」。
- 对**中小私企 / 本地小厂**，WebSearch 覆盖可能稀薄，部分维度会标注「无法评估」，这是数据可得性限制，不是框架问题。
- 硬数据（如年报参保人数）是**年度净变化**，非月度，且受社保代缴/外包干扰，已标注置信度。

---

## 红线（任何情况必须守）

1. **只读不写**：本 skill 全程只读、检索、分析，绝不发帖 / 加好友 / 私信 / 点赞。
2. **不碰密码**：若未来支持登录态检索，密码全程用户自己输入，agent 不截取、不存储。
3. **ToS 提示**：自动化访问平台可能触及条款，首次交互给一句免责提示，风险由用户自担。
4. **不内置密钥**：绝不把任何 API key 写进 skill 文件。
5. **来源标注**：报告里每条结论标明来源层级（公开口碑 L3 / 硬风险源 L1 / 用户情报 L4），不伪造、不夸大。

---

## 可视化渲染（可选，本地出 HTML + 双 SVG）

本 skill 的 `scripts/report_renderer.py` 能把按模板产出的 Markdown 报告渲染成**自包含 HTML**（内联 CSS + 风险雷达图 + 关系穿透图，离线可用、零依赖）。

用法（在 `scripts/` 目录下）：

```bash
python render_report.py 报告.md            # 输出 报告.html（同目录）
python render_report.py 报告.md -o out.html
```

前置：Python 3.8+，仅标准库。LLM 输出的 Markdown 只要含 `> 核查主体：…` 头部与 `relationship-graph` 代码块，即可被自动解析并绘图。不依赖任何后端服务。

## 引用 references

- `references/system_prompt.md` — 系统提示词（粘贴到任意 AI 平台的 System Prompt 框）
- `references/knowledge_base.md` — 风险源知识库（作为知识库上传或拼接在提示词末尾）
- `references/import_guide.md` — 元器 / Coze / Dify 等平台导入指引
- `references/risk_sources.md` — 硬风险源核查清单（查什么、在哪查、怎么解读、置信度）
- `references/search_playbook.md` — WebSearch 多源检索话术与高级语法、实体对齐提醒
- `references/interview_questions.md` — 面试反问清单（温和/直球/压力三档）+ 黑话解码提示
- `references/decision_framework.md` — 红绿灯评分维度 + 一页避坑报告模板
- `examples/example_report.md` — 输出样例报告（Markdown，含 `relationship-graph` 块，可直接用 `scripts/render_report.py` 渲染）
